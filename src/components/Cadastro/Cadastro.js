import React, { Component } from 'react';
import Form from '../FormNewUser/FormUser';

class Cadastro extends Component {
  render() {
    return (
      <Form />
    );
  }
}

export default Cadastro;
